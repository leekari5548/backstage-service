spring:
  cloud:
    nacos:
      metadata:
        management:
          context-path: ${server.servlet.context-path}/actuator
      config:
        server-addr: ${BASE_URL}:18848
        namespace: dev
        file-extension: yml
        prefix: backstage-service